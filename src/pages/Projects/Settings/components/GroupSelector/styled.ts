import { styled } from '@mui/material';

export const RootGroupSelector = styled('div')({
});

export const WrapperTitle = styled('div')({
  padding: '25px 25px 25px 0',
});

export const WrapperChilds = styled('div')({
});
